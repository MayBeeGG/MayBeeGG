name: Generate Contribution Snake Animation

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Generate Light-mode Snake
        uses: Platane/snk@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          mode: light
          # Updated Tokyo Night palette (no background color)
          palette: "414868,7aa2f7,bb9af7,9ece6a,e0af68"
          output: dist/snake-light.svg

      - name: Generate Dark-mode Snake
        uses: Platane/snk@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          mode: dark
          # Same Tokyo Night palette for dark mode
          palette: "414868,7aa2f7,bb9af7,9ece6a,e0af68"
          output: dist/snake-dark.svg

      - name: Apply Tokyo Night grid background
        run: |
          BG="#1a1b27"    # Tokyo Night background
          for f in dist/snake-*.svg; do
            # Replace grid colors
            sed -i "s/fill=\"#ebedf0\"/fill=\"${BG}\"/g" "$f"
            sed -i "s/fill=\"#161b22\"/fill=\"${BG}\"/g" "$f"
          done

      - name: Publish to output branch
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
